import { Loader } from "@/components/Loader/Loader";

export default function Loading() {
    return (
        <div className="h-screen">
            <Loader loadingPage />
            
        </div>
    )
}